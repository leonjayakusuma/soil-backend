# Database Configuration
# Required: PostgreSQL connection string (Supabase or other PostgreSQL database)
# Format: postgresql://user:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# API Security
# Required: Secret key for JWT token generation and verification
# Generate a strong random string (e.g., using: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Required: API key for protecting /api and /api/protected endpoints
# Clients must include this key in the 'x-api-key' header
API_KEY=your-api-key-here

# Server Configuration
# Optional: Port number (defaults to 3000)
PORT=3000

# Optional: Node environment (development, production, test)
# Defaults to 'development'
NODE_ENV=development

# API URL Configuration
# Optional: Custom API URL for Swagger documentation
# If not set, will use VERCEL_URL (on Vercel) or localhost:PORT (local)
# API_URL=https://your-api-domain.com

# Testing Configuration
# Optional: Base URL for API tests (defaults to http://localhost:3001)
# BASE_URL=http://localhost:3001

# Database Management
# Optional: Set to 'true' to sync database models on startup (use with caution in production)
# SYNC_DB=false

# Optional: Set to 'true' to seed database on server boot
# SEED_ON_BOOT=false

# Vercel Configuration
# Note: VERCEL_URL is automatically set by Vercel - do not set manually
# VERCEL_URL is used automatically when deployed to Vercel

# CORS Configuration
# Optional: Comma-separated list of allowed origins for CORS
# If not set, all origins will be allowed (useful for Vercel deployments)
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://your-frontend.vercel.app
# ALLOWED_ORIGINS=

